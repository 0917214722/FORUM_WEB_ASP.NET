@model IEnumerable<FORUM_WEB.Models.FrameWork.BaoCao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section styles{
    <style></style>
}
<div class="col-lg-10 col-lg-offset-2 main">
    <div class="row">
        <p>
            @*@Html.ActionLink("Create New", "Create")*@
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NoiDung)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NguoiBaoCao)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.NoiDung)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NguoiBaoCao)
                    </td>
                    <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.ID_BaoCao }) |*@
                        @Html.ActionLink("Details", "Details", new { id = item.ID_BaoCao }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ID_BaoCao })
                    </td>
                </tr>
            }

        </table>
    </div><!--/.row-->
</div>

